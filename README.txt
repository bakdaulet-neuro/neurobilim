Neuro Bilim hero update

Upload/replace these 4 files in the ROOT of the GitHub repository:
1. portrait.png          (replace the current one)
2. hero-effects.css      (new file)
3. hero-effects.js       (new file)
4. index.html            (replace the current one)

Result:
- the supplied portrait is used without changing the face;
- GPT, Gemini, Claude, Midjourney and Perplexity float upward behind the presenter;
- animation uses transform + opacity for smooth Telegram WebView performance;
- portrait is reduced from about 1 MB to about 0.33 MB;
- reduced-motion users do not get the floating animation.
